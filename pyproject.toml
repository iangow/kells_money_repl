[project]
name = "kells-money-repl"
version = "0.1.0"
description = "Replication package for money creation, inflation, asset prices, wealth distribution, and monetary policy results."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "certifi>=2024.8.30",
    "matplotlib>=3.7",
    "numpy>=1.24",
    "openpyxl>=3.1",
    "pandas>=2.0",
    "scipy>=1.10",
    "statsmodels>=0.14",
]

[tool.uv]
package = false
